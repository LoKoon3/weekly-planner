# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 앱 이름 | 주간 플래너 (Weekly Planner) |
| 컨셉 | 주간 단위 할 일 관리 + 구글 캘린더 연동 |
| 플랫폼 | PWA (모바일 앱처럼 설치 가능) |
| 언어 | 한국어 |
| 메인 폴더 | `Newapp/` |
| 배포 URL | https://lokoon3.github.io/weekly-planner/ |
| 저장소 | https://github.com/LoKoon3/weekly-planner |

> `appfile/`, `app/`, `web/` 폴더는 이전 프로젝트이며 사용하지 않음

---

## 개발 명령어

```bash
cd Newapp
npm install
npm run dev      # http://localhost:5173

# 빌드
npm run build
npm run preview
```

**TypeScript 검사**: `npx tsc --noEmit`

---

## 기술 스택

- React 19 + Vite 6
- TypeScript
- Tailwind CSS (CDN)
- Zustand (상태관리 + localStorage persist)
- Google OAuth 2.0 + Calendar API v3
- PWA (Progressive Web App)

---

## 프로젝트 구조 (Newapp/)

```
Newapp/
├── App.tsx                          # 메인 앱, 뷰 전환, 사이드바 로직
├── main.tsx                         # React 엔트리포인트
├── index.html                       # Google API 스크립트, PWA 메타 태그
├── vite.config.ts                   # Vite 설정 (GitHub Pages base 경로)
├── .env.local                       # VITE_GOOGLE_CLIENT_ID (gitignore)
│
├── components/
│   ├── Calendar/
│   │   ├── WeeklyCalendar.tsx       # 주간 뷰 (시간대 블록) - 모바일 최적화
│   │   └── DailyTimelineView.tsx    # 일별 상세 뷰 (가로 타임라인)
│   ├── Goals/
│   │   ├── YearlyGoalBanner.tsx     # 년간 목표 배너 (상단)
│   │   ├── GoalPanel.tsx            # 월간 목표 패널 (사이드바용)
│   │   └── MonthlyGoalSection.tsx   # 월간 목표 섹션 (모바일 캘린더 아래)
│   ├── Task/
│   │   └── TaskModal.tsx            # 할 일 추가/편집 모달
│   ├── Review/
│   │   └── WeeklyReviewModal.tsx    # 주간 회고 모달
│   └── common/
│       └── Modal.tsx                # 공통 모달
│
├── hooks/
│   ├── useGoogleAuth.ts             # OAuth 2.0 인증
│   └── useGoogleCalendar.ts         # 캘린더 CRUD
│
├── stores/
│   └── useStore.ts                  # Zustand 스토어
│
├── constants/
│   └── index.ts                     # 색상, 시간대, 상수
│
├── types/
│   └── index.ts                     # TypeScript 타입 정의
│
├── utils/
│   └── dateUtils.ts                 # 날짜 유틸 함수 (getHighestMonth 포함)
│
├── public/
│   ├── manifest.json                # PWA 매니페스트
│   ├── sw.js                        # Service Worker
│   └── icons/                       # 앱 아이콘 (180, 192, 512)
│
└── .github/
    └── workflows/
        └── deploy.yml               # GitHub Actions 자동 배포
```

---

## 핵심 기능 (구현 완료)

### 1. 주간 캘린더 (WeeklyCalendar.tsx)
- **시간대 블록 뷰**: 5개 시간대로 그룹화
  | 시간대 | 시간 범위 | 색상 |
  |--------|----------|------|
  | 아침 | 04:00 - 08:59 | 주황 |
  | 오전 | 09:00 - 12:59 | 파랑 |
  | 오후 | 13:00 - 17:59 | 초록 |
  | 저녁 | 18:00 - 21:59 | 보라 |
  | 밤 | 22:00 - 03:59 | 남색 |

- 날짜 헤더 클릭 → 일별 상세 뷰로 전환
- 빈 영역 클릭 → 할 일 추가 모달
- 할 일 클릭 → 편집 모달
- **모바일 반응형**: 컴팩트한 레이아웃 (sm: breakpoint 활용)

### 2. 일별 상세 뷰 (DailyTimelineView.tsx)
- **가로 타임라인**: 0-24시 (왼쪽→오른쪽)
- 겹치는 할 일은 위아래로 쌓임 (레이어 계산)
- **읽기 전용** (클릭해도 편집 불가)
- 뒤로 가기 버튼 → 주간 뷰 복귀
- 우선순위 색상 범례 표시

### 3. 할 일 관리 (TaskModal.tsx)
- CRUD (생성/조회/수정/삭제)
- 우선순위: 상(빨강)/중(노랑)/하(회색) - 자동 정렬
- 시간대 태그 5종
- 메모 필드

### 4. 구글 캘린더 연동
- **단방향 동기화**: 앱 → 구글 캘린더
- OAuth 2.0 인증 (useGoogleAuth.ts)
- 할 일 추가/수정/삭제 시 자동 동기화
- GitHub Secrets를 통한 프로덕션 환경 지원

### 5. 목표 관리
- **년간 목표 배너** (YearlyGoalBanner.tsx): 상단 그라데이션 배너
- **월간 목표**: 현재 주차에 포함된 월의 목표 표시
- **동적 월 계산**: `getHighestMonth()` - 주차에서 가장 높은 월 반환
  - 예: 6주차(2/2-2/8) → 2월 목표 표시
  - 12월/1월 경계 처리 지원

### 6. 모바일 최적화 (최신)
- **슬라이드 사이드바**: 햄버거 메뉴로 열리는 오버레이 사이드바
- **월간 목표 섹션**: 캘린더 아래 빈 공간에 표시 (모바일 전용)
- **컴팩트 헤더**: 아이콘 버튼으로 주간 회고/구글 연동
- **FAB 버튼**: 모바일에서 fixed 포지션

### 7. 기타 기능
- 주간 회고 (WeeklyReviewModal.tsx)
- 다크모드 토글
- 주간 네비게이션 (이전/다음 주)

---

## 데이터 모델 (types/index.ts)

```typescript
type Priority = 'high' | 'medium' | 'low';
type TimeTag = 'morning' | 'forenoon' | 'afternoon' | 'evening' | 'night';
type GoalType = 'yearly' | 'monthly';

interface Task {
  id: string;
  title: string;
  date: string;              // YYYY-MM-DD
  startTime: string;         // HH:MM
  endTime: string;           // HH:MM
  priority: Priority;
  timeTag?: TimeTag;
  memo?: string;
  completed: boolean;
  googleEventId?: string;    // 구글 캘린더 이벤트 ID
  createdAt: number;
  updatedAt: number;
}

interface Goal {
  id: string;
  type: GoalType;            // 'yearly' | 'monthly'
  year: number;
  month?: number;            // monthly 타입일 때만 사용
  content: string;
  createdAt: number;
}

interface WeeklyReview {
  id: string;
  year: number;
  weekNumber: number;
  completionRate: number;
  comment: string;
  createdAt: number;
}
```

---

## 주요 유틸 함수 (utils/dateUtils.ts)

```typescript
formatDate(date: Date): string           // Date → "YYYY-MM-DD"
getWeekRange(date: Date): Date[]         // 해당 주의 월~일 배열
getWeekNumber(date: Date): number        // ISO 주차 번호
formatTime(hour: number): string         // 9 → "09:00"
getTimeDecimal(timeStr: string): number  // "09:30" → 9.5
getHighestMonth(weekDays: Date[]): { month: number; year: number }  // 주차에서 가장 높은 월
```

---

## 상수 (constants/index.ts)

```typescript
// 우선순위 색상
PRIORITY_COLORS = {
  high: 'bg-red-500 text-white border-red-600',
  medium: 'bg-amber-400 text-white border-amber-500',
  low: 'bg-gray-400 text-white border-gray-500',
}

// 시간대 블록 정의
TIME_PERIODS = [
  { id: 'morning', label: '아침', startHour: 4, endHour: 8 },
  { id: 'forenoon', label: '오전', startHour: 9, endHour: 12 },
  { id: 'afternoon', label: '오후', startHour: 13, endHour: 17 },
  { id: 'evening', label: '저녁', startHour: 18, endHour: 21 },
  { id: 'night', label: '밤', startHour: 22, endHour: 3 },
]

// 유틸 함수
getTimePeriodId(timeStr: string): string  // "09:30" → "forenoon"
```

---

## 구글 캘린더 설정

### 환경 변수
```
# 로컬 개발: .env.local
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# 프로덕션: GitHub Secrets
VITE_GOOGLE_CLIENT_ID (Repository Settings → Secrets → Actions)
```

### Google Cloud Console 설정
1. OAuth 2.0 클라이언트 ID 생성 (웹 애플리케이션)
2. 승인된 JavaScript 출처:
   - `http://localhost:5173` (개발)
   - `https://lokoon3.github.io` (프로덕션)
3. OAuth 동의 화면 → 테스트 사용자 추가 (본인 이메일)
4. Calendar API 활성화

---

## PWA 설정

### 파일 구조
```
Newapp/public/
├── manifest.json          # PWA 매니페스트
├── sw.js                  # Service Worker (최소 구현)
└── icons/
    ├── icon.svg           # 원본 SVG
    ├── icon-180.png       # iOS용
    ├── icon-192.png       # Android용
    └── icon-512.png       # 스플래시용
```

### 모바일 설치 방법
1. 모바일 브라우저에서 https://lokoon3.github.io/weekly-planner/ 접속
2. **Android (Chrome)**: 메뉴 → "홈 화면에 추가"
3. **iOS (Safari)**: 공유 버튼 → "홈 화면에 추가"

---

## GitHub Pages 배포

### 자동 배포 (GitHub Actions)
- 파일: `.github/workflows/deploy.yml`
- 트리거: `main` 브랜치에 push 시 자동 실행
- 환경 변수: `secrets.VITE_GOOGLE_CLIENT_ID` 사용

### 배포 흐름
```
git add -A
git commit -m "커밋 메시지"
git push origin main
→ GitHub Actions 자동 실행 (1-2분)
→ https://lokoon3.github.io/weekly-planner/ 업데이트
```

### 주의사항
- `vite.config.ts`의 `base: '/weekly-planner/'` 설정 필수
- 저장소 이름 변경 시 base 경로도 변경 필요

---

## 해결된 이슈

| 이슈 | 원인 | 해결 |
|------|------|------|
| 할 일이 앱에 안 보임 | `formatDate()`가 UTC 사용 | 로컬 시간으로 수정 |
| 스크롤 안 됨 | `overflow-hidden` | `overflow-auto`로 수정 |
| OAuth access_denied | 테스트 사용자 미등록 | Cloud Console에서 추가 |
| GitHub Pages 404 | enablement 미설정 | `enablement: true` 추가 |
| 흰 화면 | index.css 참조 오류 | 불필요한 link 태그 제거 |
| Google Auth 오류 | CLIENT_ID 미설정 | GitHub Secrets 추가 |
| 모바일 텍스트 줄바꿈 | 고정 너비 요소 | Tailwind 반응형 클래스 적용 |
| 모바일 빈 공간 | 세로 화면 비율 | MonthlyGoalSection 추가 |
| 월간 목표 1월만 표시 | currentMonth 하드코딩 | getHighestMonth() 동적 계산 |

---

## 모바일 레이아웃 구조 (App.tsx)

```
┌─────────────────────────────────────┐
│ Header (컴팩트)                      │
│ [☰] 2026년 6주차 [<][오늘][>] [📊][G]│
├─────────────────────────────────────┤
│ YearlyGoalBanner                    │
│ ┌─────────────────────────────────┐ │
│ │ ⭐ 2026 목표 | + 목표 설정하기   │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ WeeklyCalendar                      │
│ ┌───┬───┬───┬───┬───┬───┬───┐     │
│ │월2│화3│수4│목5│금6│토7│일8│     │
│ ├───┴───┴───┴───┴───┴───┴───┤     │
│ │아침│    (시간대별 할 일)      │     │
│ │오전│                         │     │
│ │오후│                         │     │
│ │저녁│                         │     │
│ │밤  │                         │     │
│ └───────────────────────────────┘   │
├─────────────────────────────────────┤
│ MonthlyGoalSection (모바일 전용)     │
│ ┌─────────────────────────────────┐ │
│ │ 📋 2월 목표 (3개 설정됨)    [+] │ │
│ │ ① 운동 주 3회                   │ │
│ │ ② 독서 2권 완료                 │ │
│ │ ③ ...                          │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│                              [+FAB] │
└─────────────────────────────────────┘
```

### 슬라이드 사이드바 (햄버거 메뉴)
```
┌─────────────────┐
│ 주간 플래너  [X] │
├─────────────────┤
│ GoalPanel       │
│ (월간 목표)      │
│                 │
├─────────────────┤
│ [다크모드 토글]  │
│ © 2025 ...      │
└─────────────────┘
```

---

## 향후 개선 사항 (미구현)

- [ ] 반복 일정 기능
- [ ] 드래그 앤 드롭으로 할 일 이동
- [ ] 구글 캘린더 → 앱 역동기화
- [ ] 푸시 알림
- [ ] 목표 진행률 인디케이터
- [ ] 오늘의 할 일 요약 위젯

---

## 최근 업데이트 이력

| 날짜 | 내용 |
|------|------|
| 2026-01-04 | 모바일 월간 목표 섹션 추가 (빈 공간 해결) |
| 2026-01-04 | getHighestMonth() - 동적 월 계산 |
| 2026-01-04 | 모바일 슬라이드 사이드바 구현 |
| 2026-01-04 | 모바일 반응형 헤더/배너/캘린더 최적화 |
| 2026-01-04 | GitHub Actions 자동 배포 설정 |
| 2026-01-04 | PWA 설정 완료 |
